/**
 * @description Nothing. The function does nothing because it is empty (it doesn't
 * have any statements or expressions within its body).
 */
function foo() {
}

/**
 * @description Nothing; it returns no value and does not perform any actions.
 * 
 * @returns { any } The function does not return anything as it contains a "return"
 * statement without a value. Therefore the output returned by this function is none
 * or undefined.
 */
function foo() {
    return;
}

/**
 * @description The function "foo" returns undefined.
 * 
 * @returns {  } There is no output returned by the function `foo()` because it returns
 * `undefined`.
 */
function foo() {
    return undefined;
}
