
/**
 * @description Nothing; it does nothing. The function has no instructions or statements
 * and simply exists as an empty shell.
 */
function foo() {
}

/**
 * @description The function returns nothing.
 * 
 * @returns {  } The function foo returns no output or a null value.
 */
function foo() {
    return;
}

/**
 * @description The function named foo returns undefined.
 * 
 * @returns {  } Output of the function is undefined.
 */
function foo() {
    return undefined;
}
