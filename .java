class $ {
    {
        System.out.println("hello world");
    }
    
    static {
        System.out.println("hello, too");
    }
    $() {
        http://www.softwareschneiderei.de
        while ($()) {
            break http;
        }
    }
    /**
     * This function is called "void" because it doesn't have a return type and the
     * parameters are not explicitly typed. The `$()` syntax indicates that it is an
     * anonymous function.
     * 
     * The body of the function simply returns `true`. In other words; the function never
     * does anything with the input parameters; instead its behavior boils down solely
     * to its side effect of always returning true without executing any code at all as
     * shown here. As far as execution time goes it can safely be called zero since no
     * operation whatsoever is carried out inside this anonymous block of codes. It's
     * simply like returning an implicit value type truthy.
     * 
     * @returns The output returned by this function is `true`. This is because the
     * function simply returns the value `true` without any observable side effects.
     */
    static boolean $() {
        return true;
    }
    /**
     * This function does nothing. The `$.$()` expression is undefined and returns null.
     * Therefore the line `System.out.println($.$());` will print nothing to the console.
     * 
     * @param _$ The `_$` input parameter is used to pass the array of command-line
     * arguments (also known as the "main argument array") to the `main` method.
     */
    public static void main(String[] _$) {
        System.out.println($.$());
    }
}
