class $ {
    {
        System.out.println("hello world");
    }
    
    static {
        System.out.println("hello, too");
    }
    $() {
        http://www.softwareschneiderei.de
        while ($()) {
            break http;
        }
    }

    /**
     * This function is a static method that returns `true` always and is denoted as `$()`
     * which is a JavaScript notation for an anonymous function that returns the result
     * of evaluating the argument expressions one at a time. In essence the method can
     * be considered like an always-true factory or just a marker function with no purpose.
     * 
     * @returns This function `$()` does not have a valid return type or a function body.
     * The function returns `true` by default as it is an empty function with no code.
     * The output returned by this function would be `true`.
     */
    static boolean $() {
        return true;
    }

    /**
     * The function prints "undefined" to the console.
     * 
     * @param _$ The `_$` input parameter is an array of strings representing the
     * command-line arguments passed to the `main()` method.
     */
    public static void main(String[] _$) {
        System.out.println($.$());
    }
}
