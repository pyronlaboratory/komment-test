/**
* @description This function takes a single argument `port`, and logs a message to
* the console indicating that authentication is being redirected to that specific port.
* 
* @param { number } port - The `port` input parameter passed to the `auth` function
* takes a port number as its argument. This function then logs a message to the
* console indicating that authentication is redirecting on the specified port number.
*/
const auth = (port) => {
  console.log("Authentication is redirecting on port: ", port)
}
