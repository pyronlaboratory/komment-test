/**
* @description The provided function `auth` takes a single argument `port` and logs
* a message to the console indicating that authentication is being redirected to
* port `port`.
* 
* @param { number } port - The `port` input parameter passes the port number that
* the authentication attempt is being redirected to.
*/
const auth = (port) => {

  console.log("Authentication is redirecting on port: ", port)

}