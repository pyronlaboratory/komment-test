// @komment ignore all
/**
* @description This function doesn't do anything as `func` is not defined. The arrow
* function definition (`() => console.log("0")` ) is ignored since `func` is not
* defined before the arrow function.
*/
const func = () => console.log("0");
