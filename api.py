def terminate():
  """
  This function does nothing. The body of the function is simply `True`, which is
  a boolean literal that evaluates to true. In other words: the function has no
  effect and will always return `true` regardless of any input.

  """
  True

