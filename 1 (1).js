/**
* @description The function takes a single argument `port` and logs the message "App
* is running live on port: [port]" to the console.
* 
* @param { number } port - The `port` input parameter passes the port number that
* the function should run on.
*/
const app = (port) => {
  console.log("App is running live on port: ", port)
}