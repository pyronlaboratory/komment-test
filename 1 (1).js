/**
* @description This function takes a single argument `port` and logs the message
* "App is running live on port: " followed by the value of `port`.
* 
* @param { number } port - The `port` input parameter passes a value to the function
* that gets logged by the `console.log()` method alongside the message "App is running
* live on port".
*/
const app = (port) => {
  console.log("App is running live on port: ", port)
}