class Api {

/**
* @description This function logs the message "API is live (new code changes..)!".
* 
* @returns { void } The function does not return anything and only prints a message
* to the console stating that the API is live and that there are new code changes.
*/
  public static run(): void {
    console.log("API is live (new code changes..)!");
  }
}

Api.run();

