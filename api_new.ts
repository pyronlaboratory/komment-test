class Api_new {
/**
* @description This function logs a message to the console indicating that the API
* is live and new code changes have been made.
* 
* @returns { void } The output of the function `run()` is:
* 
* `console.log("API is live (new code changes..)!");`
* 
* This will log the string "API is live (new code changes..)!!" to the console.
*/
  public static run(): void {
    console.log("API is live (new code changes..)!");
  }
}

Api.run();

