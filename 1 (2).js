/**
* @description This function takes a single argument `port`, logs a message to the
* console indicating that authentication is being redirected to port `port`, and
* then... nothing.
* 
* @param { number } port - The `port` input parameter passed to the `auth` function
* simply logs a message to the console indicating the specific port being used for
* authentication.
*/
const auth = (port) => {
  console.log("Authentication is redirecting on port: ", port)





  
}