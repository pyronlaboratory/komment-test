/**
* @description The function `auth = (port) => {...}` defines an authentication
* callback function that logs a message to the console indicating that authentication
* is being redirected on a specific port `port`.
* 
* @param { number } port - In the function `auth=(port)=>{console.log("Authentication
* is redirecting on port: ",port})`, the `port` input parameter passes the value of
* the port number that the authentication is being redirected to.
*/
const auth = (port) => {
  console.log("Authentication is redirecting on port: ", port)
}