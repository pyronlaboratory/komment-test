/**
* @description The function `auth = (port) => { ...
* 
* @param { string } port - The `port` input parameter is passed to the `console.log()`
* statement and it logs the value of the `port` variable to the console.
*/
const auth = (port) => {
  console.log("Authentication is redirecting on port: ", port)
}