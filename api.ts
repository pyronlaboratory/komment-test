class Api {
/**
* @description This function logs the message "API is running live!" to the console.
* 
* @returns { void } There is no output returned by this function because it does not
* contain any statements that produce output.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}

Api.run();
