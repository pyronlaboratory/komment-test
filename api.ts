class Api {
/**
* @description This function simply logs the message "API is running live!" to the
* console.
* 
* @returns { void } There is no output returned by the function since `console.log`
* is used and does not return a value.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}

Api.run();