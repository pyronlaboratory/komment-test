class Api {
/**
* @description This function logs the message "API is running live!" to the console.
* 
* @returns { void } The output of the function is "API is running live!" since the
* `console.log()` statement within the function will log that message to the console.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}
Api.run();

