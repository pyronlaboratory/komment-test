class Api {
/**
* @description This function logs the message "API is live!" to the console.
* 
* @returns { void } The function `run()` is attempting to log the message "API is
* live!" to the console using `console.log()`.
*/
  public static run(): void {
    console.log("API is live!");
  }
}
Api.run();

