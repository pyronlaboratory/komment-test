class Api {
/**
* @description The function `run1()` logs the message "API is live!" to the console.
* 
* @returns { void } There is no output returned by the function `run1()` because it
* does not contain a return statement.
*/
  public static run1(): void {
    console.log("API is live!");
  }
/**
* @description The function `run2()` logs the message "API is live!" to the console.
* 
* @returns { void } The output returned by the `run2()` function is "API is live!"
* This is because the function calls `console.log()` with that string as its argument.
*/
  public static run2(): void {
    console.log("API is live!");
  }
/**
* @description This function logs the message "API is live!" to the console.
* 
* @returns { void } There is no output returned by the function `run3()` because it
* does not contain any statements that produce output or return values.
*/
  public static run3(): void {
    console.log("API is live!");
  }
}
Api.run1();

