class Api {
/**
* @description This function logs the message "API is running live!" to the console.
* 
* @returns { void } The output returned by this function is:
* 
* "API is running live!"
* 
* This is because the function uses the `console.log()` method to print the specified
* message to the console.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}

Api.run();

Api.run();

Api.run();

