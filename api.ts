class Api {
/**
* @description This function logs the message "API is running live!" to the console.
* 
* @returns { void } The output of the `run()` function is:
* 
* ```
* "API is running live!"
* ```
* 
* The function simply logs a message to the console indicating that the API is running.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}

Api.run();

