/**
* @description The function `log()` does nothing because it is undefined. The arrow
* function declared with no name or parameters has an empty body `()` and immediately
* returns undefined.
*/
const log = () =>  {
  console.dog("0");
}

