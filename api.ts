class Api {
/**
* @description This function simply logs the message "API is running live!" to the
* console.
* 
* @returns { void } There is no output returned by this function because it does not
* contain any statements that print or return values. The `console.log` statement
* only writes a message to the console but does not return any value.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}
Api.run();

