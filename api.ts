class Api {
/**
* @description This function logs the message "API is running live!" to the console.
* 
* @returns { void } The function `run()` does not return any value and instead outputs
* "API is running live!" to the console using `console.log()`.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}

Api.run();