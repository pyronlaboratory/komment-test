const Api {
/**
* @description This function logs the message "API is live!" to the console.
* 
* @returns { void } The output of the function `run()` is "API is live!" because the
* function logs that message to the console using `console.log()`.
*/
  public static run(): void {
    console.log("API is live!");
  }
}

Api.run();


