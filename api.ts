class Api {
/**
* @description This function simply logs the message "API is live!" to the console.
* 
* @returns { void } There is no output returned by this function because `console.log()`
* statement is used and it does not return anything.
*/
  public static run(): void {
    console.log("API is live!");
  }
}
Api.run();


