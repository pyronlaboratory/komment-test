class Api {
/**
* @description This function logs the message "API is running live!" to the console.
* 
* @returns { void } There is no output returned by the function since the `console.log()`
* statement is not inside a return statement.
*/
  public static run(): void {
    console.log("API is running live!");
  }
}

Api.run();

Api.run();

Api.run();

Api.run();
