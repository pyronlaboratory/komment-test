/**
* @description This function takes a single argument `port` and logs a message to
* the console indicating that services are being queued on that port.
* 
* @param { number } port - The `port` input parameter is a callback function that
* takes a port number as an argument and logs a message to the console indicating
* that services are being queued on that specific port.
*/
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}