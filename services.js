/**
* @description The function takes a port as an argument and logs a message to the
* console indicating that services are being queued on that port.
* 
* @param { number } port - The `port` input parameter passes a value to the
* `console.log()` function within the `services` function so that it may be included
* as part of the log message displayed to the user when the code is run.
*/
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}