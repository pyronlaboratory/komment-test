/**
* @description This function takes a single argument `port`, and logs the message
* "Services are being queued on port: <port> to the console.
* 
* @param { number } port - The `port` input parameter is a variable that takes on
* the value of the current port that services are being queued for.
*/
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}