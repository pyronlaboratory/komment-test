
/**
* @description This function takes a single argument `port` and logs the message
* "Services are being queued on port: <port>" to the console.
* 
* @param { number } port - The `port` input parameter is passed to the function as
* an argument and logs a message to the console indicating which port the service
* is being queued on.
*/
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}