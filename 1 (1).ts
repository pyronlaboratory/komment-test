class Api {
/**
* @description The function `run()` simply logs the message "API is live!" to the console.
* 
* @returns { void } The output of the function is "API is live!" because the
* `console.log()` statement within the function prints that string to the console.
*/
  public static run(): void {
    console.log("API is live!");
  }
}

Api.run();


