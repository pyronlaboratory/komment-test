
/**
 * @description (PORTS ARE BEING QUEUED ON PORT) THE FUNCTION CONSOLES
 * 
 * @param { string } port - The `port` input parameter logs a message indicating that
 * services are being queued on the specified port.
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
