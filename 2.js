
/**
 * @description The function logs a message to the console indicating that services
 * are being queued on a specific port.
 * 
 * @param { number } port - The `port` input parameter logs a message indicating that
 * services are being queued on that specific port.
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
