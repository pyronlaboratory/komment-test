
/**
 * @description The given function logs a message to the console indicating that
 * services are being queued on the specified port.
 * 
 * @param { number } port - The input `port` parameter displays a log message showing
 * that services have been queued to port .
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
