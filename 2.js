
/**
 * @description Logs the services queued to port.
 * 
 * @param {  } port - The `port` input parameter logs a message to the console
 * indicating that services are being queued on the specified port.
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
