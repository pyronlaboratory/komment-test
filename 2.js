
/**
 * @description Logs message "Services are being queued on port: <port>".
 * 
 * @param { number } port - The `port` input parameter is queued.
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
