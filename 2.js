
/**
 * @description logs a message to the console indicating that services are being
 * queued on the specified port
 * 
 * @param { object } port - The `port` input parameter is being passed to `console.log()`
 * as an argument for logging purposes.
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
