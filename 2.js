
/**
* @description This function takes a single argument `port` and logs a message to
* the console indicating that services are being queued on that port.
* 
* @param { number } port - The `port` input parameter is passed to the function and
* simply logged to the console.
*/
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}