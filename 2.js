
/**
 * @description port number is logged to the console using the `console.log()` method
 * alongside with a message detailing the process of port queueing.
 * 
 * @param { number } port - The `port` input parameter logs a message to the console
 * indicating that services are being queued on that specific port.
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
