
/**
 * @description Logs the message "Services are being queued on port:" and a variable
 * containing a value representing the current active service's port.
 * 
 * @param { number } port - Here's an explanation of how "port" affects this function
 * below.
 * 
 * port input paramter passes a variable whose values log messages get logged on to
 * the console as stated
 */
const services = (port) => {
    console.log("Services are being queued on port: ", port)
}
