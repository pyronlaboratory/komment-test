def server(port):
  """
  The function `server(port)` prints a message to the console indicating that the
  server is listening on the specified port.

  Args:
      port (int): The `port` input parameter specifies the TCP port number that
          the server should listen on for incoming connections.

  """
  print(f"Server listening on port: {port}")
