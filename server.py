def server():
  """
  This function defines a Python function called `server` that prints a message
  indicating that backend services are running on port 8889.

  """
  port = 8889
  print(f"Backend services running on port #{port}")
