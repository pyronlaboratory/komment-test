def server(port):
  """
  This function prints a message to the console indicating that it is listening
  for incoming requests on a specific port.

  Args:
      port (int): The `port` input parameter passed to the `server()` function
          determines which port number the server will listen on for incoming requests.

  """
  print(f"Server listening on port: {port}")
