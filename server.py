def server():
  """
  The function `server()` defines a simple network server that listens on port
  8889 and prints a message indicating that the backend services are running on
  that port.

  """
  port = 8889
  print(f"Backend services running on port #{port}")
