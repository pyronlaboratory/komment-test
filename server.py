def server(port):
  """
  The function `server(port)` simply prints a message indicating that the server
  is listening on the specified port `port`.

  Args:
      port (int): The `port` input parameter defines the TCP port number that the
          server should listen on and bind to.

  """
  print(f"Server listening on port: {port}")
