def server(port):
  """
  The `server` function listens for incoming requests on a specific port (which
  is passed as an argument) and prints a message indicating that the server is running.

  Args:
      port (int): The `port` input parameter specifies which port the server should
          listen on.

  """
  print(f"Server listening on port: {port}")
