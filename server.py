def server(port):
  """
  This function defines a server that listens for incoming requests on a specified
  port.

  Args:
      port (int): The `port` input parameter defines the port number that the
          server should listen on.

  """
  print(f"Server listening on port: {port}")
