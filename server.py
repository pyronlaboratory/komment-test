def server():
  """
  This function defines a simple server that listens on port 8889 and prints a
  message to the console indicating that the backend services are running.

  """
  port = 8889
  print(f"Backend services running on port #{port}")
