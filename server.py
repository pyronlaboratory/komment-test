def server(port):
  """
  This function takes a port number as an argument and prints a message indicating
  that the server is listening on that port.

  Args:
      port (int): The `port` input parameter is a numeric argument that determines
          which port number the `server` function will listen on for incoming
          network requests.

  """
  print(f"Server listening on port: {port}")
