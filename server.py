def server(port):
  
  """
  This function declares a server that listens on a specific port (as indicated
  by the `port` argument passed to the function).

  Args:
      port (int): The `port` input parameter specifies the port number that the
          server should listen on.

  """
  print(f"Server listening on port: {port}")
