def server():
  """
  This function declares a Python function named `server` that prints a message
  to the console indicating that the backend services are running on port 8889.

  """
  port = 8889
  print(f"Backend services running on port #{port}")
