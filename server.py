def server(port):
  """
  This function prints the message "Server listening on port: [PORT]" to the console
  when it is called with a port number as an argument.

  Args:
      port (int): The `port` input parameter is a numerical value that tells the
          function which port to listen on for incoming requests.

  """
  print(f"Server listening on port: {port}")
