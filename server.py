def server(port):
  """
  This function prints a message to the console indicating that a server is listening
  on a specific port.

  Args:
      port (int): The `port` input parameter passed to the `server()` function
          determines which port number the function should listen on for incoming
          network requests.

  """
  print(f"Server listening on port: {port}")
