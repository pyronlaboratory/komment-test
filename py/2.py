def server(port):
  """
  The server function listens for incoming requests on a given port and prints a
  message indicating that it is ready to receive connections and mentions the
  specific port it is listening on.

  Args:
      port (str): The `port` input parameter specifies the port on which the server
          should listen for incoming connections. It is passed to the `print()`
          function to display a message indicating that the server is listening
          on that port.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
