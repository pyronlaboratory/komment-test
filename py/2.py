def server(port):
  
  """
  The `server()` function listens for incoming requests on a specified port and
  prints a message indicating that it is ready to accept connections.

  Args:
      port (int): The `port` input parameter determines which port number the
          server will use to listen for incoming connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed");

