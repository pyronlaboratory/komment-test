def server(port):
  """
  This Python function defines a simple web server that listens on a specific port
  and prints a message to the console indicating that it is running and ready to
  receive requests.

  Args:
      port (int): The `port` parameter determines which port number the server
          will listen on for incoming requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
