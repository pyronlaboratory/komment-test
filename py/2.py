def server(port):
  """
  The function "server(port)" simply prints a message indicating that the server
  is active and accepting incoming connections on port "port".

  Args:
      port (int): The `port` input parameter specifies which port on the computer
          that the server should be operating on.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed");

