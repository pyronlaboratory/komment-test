def server(port):

  """
  The function "server" listens on a given port and prints a message indicating
  that it is running and able to accept connections.

  Args:
      port (int): The `port` input parameter indicates the port number that the
          server will listen on and receive incoming requests from clients.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
