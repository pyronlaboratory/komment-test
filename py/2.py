def server(port):

  """
  This function named `server` takes an input `port` and prints a message indicating
  that the server is active and ready to listen for incoming connections on that
  port.

  Args:
      port (str): The port input parameter is a server's "address" which allows connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
