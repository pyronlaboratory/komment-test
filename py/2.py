def server(port):
  """
  This function runs a server that listens for incoming connections on a specified
  port and prints a message indicating that it is ready to receive connections.

  Args:
      port (int): The `port` parameter defines the port where the server listens
          for incoming client requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
