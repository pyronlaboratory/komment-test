def server(port):
  
  """
  The function `server` listens on a specific port and prints a message indicating
  that it is waiting for incoming requests.

  Args:
      port (int): The `port` parameter determines the port number on which the
          server listens for incoming client requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
