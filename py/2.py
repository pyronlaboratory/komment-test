def server(port):
  """
  The given function defines a `server` which listens on a specific port and prints
  a message to the console indicating that it is active and ready to receive
  incoming connections.

  Args:
      port (int): The `port` input parameter tells the server to listen for incoming
          connections on that specific port.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
