def server(port):

  """
  This server listens on a specified port and prints a message indicating that it
  is ready to receive connections.

  Args:
      port (int): The `port` parameter tells the server which port to bind to when
          accepting incoming client connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
