def server(port):
  
  """
  This Python function implements a network server that listens for incoming
  connections on a specified port ( Parameter). The function prints a message
  indicating that the server is listening and ready to accept connections. The
  output of this server will run simultaneously with adding new features parallel
  channels can change modes output.

  Args:
      port (int): The input parameter `port` defines which port to bind the server
          function to; the default is port 8080 when none is given.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
