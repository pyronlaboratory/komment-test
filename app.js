/**
* @description This function doesn't do anything since `undefined` is not a function
* and nothing gets executed when it's called.
*/
const app = () => {
  console.log("App is running live!");
};






