/**
* @description The function app creates an anonymous function that outputs the message
* "App is running live!" to the console once.
*/
const app = () => {
  console.log("App is running live!");
};

