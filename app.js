
/**
 * @description The function executes the code inside the arrow function () => { when
 * the application is running. It logs the message "App is running live!" to the console.
 */
const app = () => {
  
  console.log("App is running live!");
};
