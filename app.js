
/**
* @description This function takes a single argument `port` and logs a message to
* the console indicating that the app is running on that specific port.
* 
* @param { number } port - The `port` input parameter is passed to the function and
* Logged to the console as a message along with the fact that the app is running
*/
const app = (port) => {

  console.log("App is running live on port: ", port)
}