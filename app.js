/**
* @description This function defines a trivial function `app` that simply logs the
* message "App is running live!" to the console.
*/
const app = () => {
  console.log("App is running live!");
};