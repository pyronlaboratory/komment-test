
/**
* @description This function takes a single argument `port`, and logs the message
* "App is running live on port: " followed by the value of `port` to the console.
* 
* @param { number } port - The `port` input parameter passed to the function app
* enables the function to recognize the specific port where the application will run
* and then output a message stating that it is running on the specific identified port.
*/
const app = (port) => {

  console.log("App is running live on port: ", port)
}