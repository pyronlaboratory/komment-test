/**
* @description The function takes a single argument `port`, and logs a message to
* the console indicating that the app is running live on that port.
* 
* @param { any } port - The `port` input parameter is a parameter passed to the
* function and is used to determine which port number the app should run on.
*/
const app = (port) => {
  console.log("App is running live on port: ", port)
}