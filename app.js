/**
* @description The function defines an immediately invoked function expression (IIFE)
* that logs the message "App is running live!" to the console once.
*/
const app = () => {
  console.log("App is running live!");
};