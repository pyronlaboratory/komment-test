/**
* @description The function creates an empty function expression and then logs the
* message "App is running live!" to the console using `console.log()`.
*/
const app = () => {
  console.log("App is running live!");
};


