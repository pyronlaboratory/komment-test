/**
* @description The given function takes a single argument `port` and logs a message
* to the console indicating that the app is running live on that port.
* 
* @param { number } port - The `port` input parameter is a function argument that
* receives a value as an integer and logs a message to the console indicating that
* the app is running live on that specific port.
*/
const app = (port) => {

  console.log("App is running live on port: ", port)

}