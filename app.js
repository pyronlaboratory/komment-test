/**
* @description The function app returns undefined and immediately logs "App is running
* live!" to the console without returning anything or taking any input.
*/
const app = () => {
  console.log("App is running live!");
};