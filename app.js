/**
* @description This function takes a single argument `port` and logs a message to
* the console indicating that the app is running live on that port.
* 
* @param { number } port - The `port` input parameter passed to the function takes
* a single argument (`number`) and log's it with a message "App is running live on
* port:" followed by the port number.
*/
const app = (port) => {

  console.log("App is running live on port: ", port)

}