/**
* @description The function takes a single argument `port`, and logs the message
* "App is running live on port: " followed by the value of `port` to the console.
* 
* @param { number } port - The `port` input parameter specifies the port number where
* the app will listen for incoming requests.
*/
const app = (port) => {
  console.log("App is running live on port: ", port)
}