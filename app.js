/**
* @description This function creates an empty functional component that logs the
* message "App is running live!" to the console when the app is loaded.
*/
const app = () => {
  console.log("App is running live!");
};


