/**
* @description The function takes a single argument `port`, and logs the message
* "App is running live on port: <port>" to the console.
* 
* @param { number } port - The `port` input parameter is passed as an argument to
* the function and it allows the function to specify the port number that the app
* should run on.
*/
const app = (port) => {
  console.log("App is running live on port: ", port)
}