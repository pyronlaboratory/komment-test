/**
* @description The function takes a `port` argument and logs the message "App is
* running live on port: [port number]" to the console.
* 
* @param { number } port - The `port` input parameter passes a value to the function
* that the app will run on.
*/
const app = (port) => {

  console.log("App is running live on port: ", port)

}