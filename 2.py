def server(port):
  """
  The function `server(port)` prints a message indicating that the server is
  listening on port `port` and is able to accept incoming connections.

  Args:
      port (int): The `port` input parameter is used to specify the port number
          on which the server will listen for incoming requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
