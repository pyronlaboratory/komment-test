def server(port):
  """
  This function runs a server that listens on a specified port and prints a message
  indicating that the server is active and accepting incoming connections.

  Args:
      port (int): The `port` input parameter is used to specify the port number
          on which the server will listen for incoming connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
