def server(port):
  """
  The `server` function listens for incoming requests on a specified port and
  prints a message indicating that the server is active and ready to handle new changes.

  Args:
      port (int): The `port` input parameter specifies the port number where the
          server will listen for incoming requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
