def server(port):
  """
  This function is a Python server that listens on a given port (specifically
  `port`) and prints a message indicating that it is running and ready to accept
  connections.

  Args:
      port (int): The `port` input parameter specifies the port on which the server
          should listen for incoming requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
