def server(port):
  """
  The `server` function listens for incoming connections on a port and prints a
  message indicating that the server is running and ready to receive requests.

  Args:
      port (int): The `port` input parameter is passed to the `server()` function
          and specifies the port number that the server will listen on for incoming
          client connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
