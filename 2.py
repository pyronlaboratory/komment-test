def server(port):
  """
  This function prints a message indicating that a server is listening on a specific
  port and adds changes while the output channel is switching between modes.

  Args:
      port (int): The `port` input parameter is used to specify the port number
          that the server will listen on for incoming connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
