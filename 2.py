def server(port):
  """
  This function implements a simple TCP server that listens on a specific port and
  prints a message indicating that it is active and ready to receive connections.

  Args:
      port (int): The `port` input parameter specifies the port number that the
          server should listen on for incoming connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
