def server(port):
  """
  The `server` function listens for incoming requests on a specified port and
  prints a message indicating that the server is ready to accept connections.

  Args:
      port (int): The `port` input parameter is used to specify the port number
          that the server should listen on.

  """
  print(f"Server listening: {port}")
