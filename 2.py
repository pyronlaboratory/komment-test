def server(port):
  """
  The given function "server(port)" prints a message indicating that the server
  is listening on the specified port.

  Args:
      port (int): The `port` input parameter is used to specify the port number
          that the server should listen on.

  """
  print(f"Server listening: {port}")

