def server(port):
  """
  The `server` function listens for incoming requests on a specific port and prints
  a message indicating that it is ready to receive connections.

  Args:
      port (int): The `port` input parameter specifies the port number on which
          the server should listen for incoming requests.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
