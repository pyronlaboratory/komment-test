def server(port):
  """
  This function simply prints a message to the console indicating that the server
  is listening on the specified port and that it will be adding new changes while
  the output channel mode is changed.

  Args:
      port (int): The `port` input parameter specifies the port on which the server
          will listen for incoming connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
