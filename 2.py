def server(port):
  """
  This function is a simple server that listens on a given port and prints a message
  indicating that it is active and ready to receive incoming requests.

  Args:
      port (int): The `port` input parameter is used to specify the port number
          that the server should listen on. It is passed as an argument to the
          `server` function and is printed along with a message indicating that
          the server is listening on that port.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")
