def server(port):
  """
  This function listens for incoming connections on a given port and prints a
  message indicating that the server is listening on that port.

  Args:
      port (int): The `port` input parameter specifies the port number that the
          server should listen on.

  """
  print(f"Server listening on port: {port}")
