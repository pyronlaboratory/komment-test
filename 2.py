def server(port):
  """
  The function "server" listens for incoming requests on a specific port (parameter)
  and prints a message indicating that it is ready to receive connections.

  Args:
      port (int): The `port` input parameter tells the function which port number
          to listen on for incoming requests.

  """
  print(f"Server listening: {port}")


