def server(port):

  """
  This function simply prints a message indicating that a server is listening on
  the specified port.

  Args:
      port (int): The `port` input parameter passes a value to the function that
          tells the function what port to listen on for incoming client connections.

  """
  print(f"Server listening on port: {port}")
