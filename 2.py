def server(port):




  """
  The function "server" takes a port as input and prints a message indicating that
  the server is listening on that port and that it can handle new changes concurrently.

  Args:
      port (int): The `port` input parameter specifies the port number on which
          the server will listen for incoming connections.

  """
  print(f"Server listening: {port} | adding new changes in parallel while output channel mode changed")


