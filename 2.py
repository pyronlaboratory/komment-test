def server(port):

  """
  This function defines a simple server that listens on a specified port (passed
  as an argument) and prints a message indicating that the server is running.

  Args:
      port (int): The `port` input parameter tells the `server` function which
          port to listen on for incoming connections.

  """
  print(f"Server listening on port: {port}")
