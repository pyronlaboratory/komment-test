def server(port):
  """
  This function prints the message "Server listening: {port}" to the console.

  Args:
      port (int): The `port` input parameter specifies which port the server should
          listen on.

  """
  print(f"Server listening: {port}")
